{
  "name": "pw_test_project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "mobile": "npx playwright test --project mobile --headed",
    "smoke": "npx playwright test --project chromium --headed --grep @smoke",
    "locators": "npx playwright test locators.spec.ts --project chromium",
    "ui": "npx playwright test --project chromium --headed --grep @ui",
    "allure-generate": "allure generate ./allure-results --clean -o ./allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.13.13",
    "allure-playwright": "^3.2.2"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  }
}
